<%= form_with(model: atricle) do |form| %>
  <% if atricle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atricle.errors.count, "error") %> prohibited this atricle from being saved:</h2>

      <ul>
        <% atricle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
